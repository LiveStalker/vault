<h2>Edit item</h2>

<%= labelled_form_for @sitem, :url => project_vaults_path + '/' + @vault_id.to_s + '/sitems/' + @sitem.id.to_s, :html => {:id => 'sitem-form'} do |f| %>
    <%= render :partial => 'form', :locals => {:f => f} %>
    <%= submit_tag 'Update' %>
<% end %>
